name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

# Serialize releases so concurrent pushes compute distinct patch numbers.
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Use a token that can push tags back.
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute next CalVer version
        id: version
        run: |
          set -euo pipefail
          git fetch --tags --force
          DATE="$(date -u +'%Y.%m.%d')"
          LAST_PATCH=$(git tag --list "v${DATE}.*" \
            | sed -E "s/^v${DATE}\.//" \
            | sort -n | tail -n1 || true)
          if [ -z "${LAST_PATCH:-}" ]; then
            NEXT_PATCH=0
          else
            NEXT_PATCH=$((LAST_PATCH + 1))
          fi
          VERSION="${DATE}.${NEXT_PATCH}"
          TAG="v${VERSION}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Next release: ${TAG}"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          VITE_APP_VERSION: ${{ steps.version.outputs.version }}
          REACT_APP_VERSION: ${{ steps.version.outputs.version }}
          # No VITE_DEPLOY_TARGET -> defaults to prod (base '/', writes CNAME).
        run: npm run build

      - name: Package build artifact
        run: |
          set -euo pipefail
          TAG="${{ steps.version.outputs.tag }}"
          if [ ! -d docs ]; then
            echo "::error::Expected build output at docs/ (set by vite.config.js outDir)"
            exit 1
          fi
          tar -czf "build-${TAG}.tar.gz" -C docs .

      - name: Create and push tag
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git tag -a "$TAG" -m "Release $TAG"
          git push origin "$TAG"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          set -euo pipefail
          gh release create "$TAG" \
            --title "$TAG" \
            --generate-notes \
            "build-${TAG}.tar.gz"
